# searchcli
    

    searchcli -f tokens.json < /path/to/text/document.txt

where `tokens.json` is in the form:
    
    [
      {
        'id': 1,
        'label': 'string',
        'category': 'string'
      },
      .
      .
      .
    ]

